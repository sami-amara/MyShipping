This folder contains backups of original module files saved before automated refactors.

To restore original files, run the PowerShell restore script at `UI/scripts/restore-modules-backup.ps1`.

Files are named `<originalName>.orig.js` or `.orig.json`.
